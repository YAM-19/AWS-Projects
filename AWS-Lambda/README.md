# AWS Lambda

## Overview

AWS Lambda is a **serverless compute service** that allows us to run code without managing servers.

AWS automatically manages the infrastructure, scaling, and resources needed to run the function.

## What I Learned

* What AWS Lambda is
* Serverless architecture
* Lambda vs EC2
* Lambda functions
* Lambda Handler
* Function URLs
* Lambda testing
* Basic IAM permissions
* How Lambda can be used for DevOps automation

## Practical Work

I created a **Python-based AWS Lambda function** using the AWS Management Console.

I also enabled a **Lambda Function URL** and tested the function through a web browser.

## Result

The Lambda function was successfully deployed and returned:

`Hello from Lambda!`

## AWS Service Used

* AWS Lambda

## Screenshot

The `screenshots` folder contains the Lambda Function URL output as proof of successful execution.

## Key Takeaway

**AWS Lambda = Run code without managing servers.**

It is useful for event-driven applications, automation, monitoring, and DevOps tasks.
